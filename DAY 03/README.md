Ethical Hacking & Kali Linux Notes

---

Key Terms

1. Static and Dynamic Analysis  
**Static Analysis** refers to examining code without executing it.  
It is used to detect vulnerabilities in source code or binaries.  

**Dynamic Analysis** involves analyzing a program while it is running.  
It helps observe runtime behavior and identify issues like memory leaks or unauthorized access.

---

### 2. File Types  
**.exe** – Executable file used in Windows operating systems.  
**.apk** – Android Package Kit used for installing apps on Android.  
**.gif** – Image file format that supports animation.

---

3. ATS Software  
ATS stands for Application Tracking System.  
It is used by companies to scan, filter, and rank job applications or resumes before human review.

---

4. Obfuscation  
Obfuscation is a technique used to make code hard to understand.  
It is often used in malware to hide its behavior or to protect software from being reverse-engineered.

---

5. Open Source  
Open source software has its source code publicly available.  
This means anyone can view, modify, and distribute the code.  
Examples include Linux, Nmap, and Metasploit.

---

## Kali Linux: Basic Terminal Commands

| Command | Description |
|---------|-------------|
| `ls` | List contents of the current directory |
| `tree` | Show directory structure in a tree format |
| `cd <dir>` | Change the current directory |
| `pwd` | Print the current directory path |
| `mkdir <name>` | Create a new directory |
| `rmdir <name>` | Remove an empty directory |
| `touch <file>` | Create an empty file |
| `rm <file>` | Remove a file |
| `cp <src> <dest>` | Copy a file or directory |
| `mv <src> <dest>` | Move or rename a file or directory |
| `echo <text>` | Print a message to the terminal |
| `chmod <perm> <file>` | Change file permissions |
| `chown <user>:<group> <file>` | Change file ownership |
| `ps` | List active processes |
| `kill <PID>` | Terminate a process by process ID |
| `top` | Display real-time processes and system usage |

---

## Nmap (Network Mapper)

Nmap is a free and open-source tool used for network discovery and security auditing.  
It can detect live hosts, open ports, running services, and operating systems on a target network.
